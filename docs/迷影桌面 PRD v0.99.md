# 迷影桌面 PRD v0.99

> 文档状态：Skill 发布候选版  
> 当前日期：2026-05-28  
> 产品形态：跨 agent 的电影海报 / 壁纸生成 skill  
> 当前结论：skill 进入对外发布候选状态；默认 quick mode，用最少确认完成电影海报/壁纸生成，最终只交付海报。

## 1. 产品定义

迷影桌面 v0.99 是一个可配置在各类 AI agent 中的技能。用户提供真实电影名，skill 用默认快速模式自动推断尺寸、输出路径、文字模式、生图路径、风格赛道和必要参考图，生成原创电影海报/壁纸。默认用户只看到最终海报和文件路径。

一句话定义：

> 请告诉我您想看到哪部电影的海报？

## 2. 核心变化

- 不内置“定时换桌面壁纸”功能。Windows 和 macOS 已有系统级定时换壁纸能力。
- 不默认抓取豆瓣或海报站现成海报。
- 不把抽象美感放在电影识别度之上。
- 重点从“桌面工具”转为“电影海报/壁纸生成向导”。
- 优先自动推断尺寸、输出目录、文字版本后生成；只有无法安全推断时才询问。
- 必须根据影片自身影调决定亮暗、色彩、对比度和材质，不默认阴暗。
- 必须通过真实角色视觉锚点增强识别度，避免反复生成远景背影小人。
- 支持在用户确认后记忆默认尺寸和输出目录。
- 默认最终交付只包含海报/壁纸文件和极简状态，不展开研究报告、视觉 brief、manifest、提示词或负面提示词。
- 提示词包和研究信息仍保存到文件，供复现和调试；只有用户要求、prompt-only 模式或失败排查时才展示。
- Skill 运行应节省 token：少解释、少中间汇报、少文档输出，优先完成图片。
- 默认启用 quick mode：能自动推断或使用偏好的设置，不再询问用户。
- 只在电影歧义、缺少必要权限/API、外部付费风险、参考图失败、路径不可写或用户明确要求时提问。

## 3. 用户引导

加载 skill 后，第一句话必须简洁：

```text
请告诉我您想看到哪部电影的海报？
```

之后尽量集中确认：

1. 电影身份  
   如果电影名有歧义，列出候选并请用户确认。

2. 快速默认  
   电影确认后，默认不再询问设置。优先使用已保存偏好、当前屏幕、默认输出目录、带文字版、可用生图工具和风格路由器。

3. 必要时一轮设置  
   只有无法安全推断时，才把尺寸/设备、保存位置、文字版本、生图方式集中在一轮对话中询问，不拆成多轮。

4. 目标尺寸  
   支持：
   - 明确尺寸，如 `3840x2160`；
   - 当前设备屏幕；
   - 手机/平板/电脑型号；
   - 通用比例，如 `16:9`、`9:16`、`4:5`。

5. 设备分辨率查询  
   如果用户要做手机壁纸但不知道尺寸，优先从上下文或偏好推断设备；无法推断时才请求具体型号。查询结果置信度高时直接使用，来源冲突或不确定时才确认。

6. 输出目录  
   优先使用保存过的输出目录；没有时使用默认 `CinephileWallpaper/outputs/YYYY/MM`。只有路径不可写、不安全或用户指定新路径时才询问。

7. 文字版本  
   默认生成带文字版。用户可指定：
   - 带文字；
   - 无文字；
   - 两者都要。

   带文字版本默认优先由生图模型直接生成完整海报，以提升效率。只有在用户需要精确文字、模型文字失败、批量一致排版或标题过长时，才走后期排版。

8. 生图方式  
   默认按顺序自动路由：已保存自动模式、当前 agent 生图能力、其他生图 skill/tool、已配置外部 API、prompt-only。只有缺少凭据、涉及明显费用差异或用户要求选择时才询问。

## 4. 视觉策略

默认生成“电影海报感”的原创视觉，而不是写实剧照。

所有生成图都应是美术风格海报，不使用真人写实风格。可使用油画、素描、水粉、丙烯、水彩、版画、现代主义、印象派、野兽派、抽象主义、波普艺术、构成主义、拼贴等美术/平面设计语言。

v0.8 起，视觉风格不应停留在“正常插画”或常规电影概念图。Skill 必须具备更大的艺术史与当代艺术调度空间，能根据影片选择现当代艺术、古典艺术、地域传统、实验材料，或有意识的反差风格。

如果用户反馈生成结果仍然常规、插画化、太安全，下一次生成必须启用 `style_escalation`，通过更激进的艺术语言、抽象机制和材料约束改变图像结构，而不是简单增加“更艺术、更大胆”等形容词。

### 4.1 艺术提炼

Skill 应先完成：

1. 剧情事实；
2. 情绪压力；
3. 中心隐喻；
4. 海报装置；
5. 艺术语言；
6. 影片影调；
7. 角色锚点。

### 4.1.1 艺术语言系统

Skill 应为每次生成选择：

- 一个明确的 `style_lane`；
- 一个主艺术语言；
- 一个辅助材料或视觉装置；
- 一个抽象机制；
- 一组能指/所指层次；
- 一个风格选择理由。

不要简单堆叠流派名。风格选择必须说明它如何揭示影片，而不是只让画面“好看”。

默认不能把 `fine-art poster`、`painterly`、`cinematic`、`beautiful illustration`、`movie poster style` 当成主风格。它们只能作为辅助描述，不能作为风格系统。

如果存在历史记录，连续三次成功生成不得重复同一个 `style_lane` 或同一个大类 `movement_family`，除非用户明确指定。

`style_lane` 可选：

- `geometric_avant_garde`：构成主义、至上主义、包豪斯、风格派、瑞士网格；
- `fragmented_modernism`：立体主义、未来主义、多视角断裂、分析拼贴；
- `expressive_color`：野兽派、表现主义、新表现主义、非自然情绪色彩；
- `pop_repetition_media`：波普、丝网印刷、半调、重复头像/物件、消费图像批评；
- `conceptual_dada_surreal`：达达、超现实、观念置换、不可能物件；
- `optical_digital_signal`：欧普艺术、动力网格、CRT、VHS、数字故障、信号噪声；
- `material_arte_povera`：灰烬、布、金属、纸、绳、尘土、现成物装置；
- `east_asian_ink`：中国水墨、文人画、日本画、笔触、留白和纸性；
- `gongbi_miniature`：工笔、波斯/印度细密画、矿物色、密集符号；
- `ukiyo_e_flatworld`：浮世绘、平面色块、织物纹样、季节性构图；
- `medieval_icon_glass`：中世纪手抄本、拜占庭圣像、哥特彩窗、金色平面；
- `renaissance_baroque_allegory`：湿壁画、古典寓言、巴洛克光线、舞台式人物；
- `social_mural_realism`：列宾派、墨西哥壁画、苏联宣传画、公共历史叙事；
- `print_process`：木刻、亚麻油毡版画、铜版蚀刻、石版画、risograph、丝网印刷；
- `textile_tapestry_map`：挂毯、刺绣、编织网格、地图/图解结构。

提示词必须让 `style_lane` 控制构图、材质和抽象机制，而不是把风格当装饰性形容词。

可选艺术语言包括但不限于：

现当代艺术：

- 抽象派；
- 抽象表现主义；
- 色域绘画；
- 构成主义；
- 至上主义；
- 立体主义；
- 未来主义；
- 野兽派；
- 达达；
- 超现实主义；
- 包豪斯；
- 风格派；
- 波普艺术；
- 欧普艺术；
- 极简主义；
- 观念艺术；
- 新表现主义；
- 贫穷艺术；
- 粗野主义平面设计；
- 后现代拼贴；
- 数字故障 / vaporwave。

经典、古代与地域传统：

- 欧洲中世纪手抄本；
- 拜占庭圣像；
- 哥特彩窗；
- 文艺复兴湿壁画；
- 巴洛克明暗法；
- 新古典主义寓言画；
- 浪漫主义风景；
- 俄罗斯现实主义 / 列宾派；
- 日本浮世绘；
- 日本画；
- 中国水墨写意；
- 中国文人画；
- 中国工笔画；
- 藏传唐卡结构；
- 波斯细密画；
- 印度细密画；
- 新艺术运动；
- 装饰艺术；
- 苏联宣传画；
- 墨西哥壁画。

材料与工艺：

- 木刻；
- 石版画；
- 丝网印刷；
- risograph；
- 铜版蚀刻；
- 撕纸拼贴；
- 织物/挂毯；
- 陶瓷釉色；
- 金箔；
- 木炭与粉彩；
- 湿壁画墙面；
- 彩色玻璃；
- 蓝图/技术图纸；
- 档案复印；
- 热敏打印；
- VHS / CRT 影像噪声。

可吸取经典与当代艺术家的设计语法，例如安迪·沃霍尔式的重复图像与消费符号、大卫·霍克尼式的明亮空间与拼贴视角、罗斯科式色域压力、康定斯基式音乐抽象、马列维奇式几何还原、毕加索/布拉克式立体主义破碎、马蒂斯式野兽派色彩、杜尚式观念置换、博伊斯式材料仪式、草间弥生式重复图案、白南准式视频信号等。

涉及在世艺术家时，不直接要求“模仿某某风格”，而是蒸馏其可观察的视觉操作：色彩、构图、材料、重复、符号、空间、节奏、观念机制。

### 4.1.2 反差风格

Skill 可以有意识地使用反差风格，但必须服务影片主题。

示例：

- 科幻电影可转译为中国水墨、浮世绘、中世纪手抄本或文艺复兴寓言；
- 超级英雄电影可转译为构成主义几何、波普系统、湿壁画或中世纪战争挂毯；
- 社会现实电影可转译为工笔静物、贫穷艺术材料或观念艺术装置；
- 恐怖片可转译为极简抽象、彩窗或冷峻包豪斯网格；
- 爱情片可转译为立体主义双重视角、色域绘画或浮世绘季节构图。

反差风格必须写出桥接理由：

```text
虽然这部电影属于 [类型/时代]，但 [艺术语言] 可以通过 [视觉机制] 揭示 [主题/情绪/结构]。
```

不能把文化风格当装饰，也不能为了猎奇牺牲电影识别度。

### 4.1.3 风格升级机制

当输出像普通插画、概念图或安全电影海报时，skill 必须进行结构性升级：

1. 从常规海报设计升级为明确艺术流派；
2. 从风格标签升级为抽象机制；
3. 从表面纹理升级为材料/工艺约束；
4. 从好看升级为能指/所指层次；
5. 写明该艺术语言为什么揭示这部电影。

以《色，戒》为例，不应只生成优雅民国时期插画。可考虑：

- 麻将立体主义：脸、手、旗袍、麻将牌、监视空间被切成多视角平面；
- 工笔间谍细密画：丝绸、匕首、戒指、窗格与压抑情欲形成精密秩序；
- 沃霍尔式重复身份：重复面具、口红、红印章和商业色彩被转化为焦虑；
- 构成主义欲望陷阱：红黑斜线、身体向量、政治几何和隐秘代码；
- 水墨 noir 反差：身体消融为墨迹，戒指或烟头成为唯一饱和符号。

### 4.2 影片影调

Skill 必须先判断影片自身的影像语言，再决定壁纸的亮暗、色彩、对比度和材质，不应把“阴暗”“低饱和”“黑底”当成严肃电影的默认答案。

需要研究并记录：

- 彩色 / 黑白 / 混合 / 动画；
- 高调、中调、低调或混合影调；
- 对比度：柔和、硬朗、扁平、明暗法、图形化等；
- 色温：暖、冷、中性或混合；
- 胶片、数字、动画、纪录片、档案影像等媒介线索；
- 摄影师或影片公认视觉风格；
- 影片气质与类型：青春、喜剧、爱情、日常、宗教性、都市疏离、动作奇观、悲剧、荒诞等。

可选影调策略包括：

- 明亮暖调；
- 日常自然色；
- 粉彩忧郁；
- 日晒褪色；
- 黑白图形；
- 土色圣像；
- 高饱和波普；
- 霓虹/金属奇观；
- 发光的精神性；
- 冷调制度空间；
- 夜曲低调。

低调、黑底、阴郁影调只在影片本身需要时使用。比如动作奇观、青春喜剧、暖色日常片、彩色动画、明亮外景片，不应被统一压成暗调。

### 4.3 角色锚点

如果生成图与电影联系不强，必须加入主人公或关键人物视觉锚点。

角色锚点不应只是一种远景背影。应支持多种呈现：

- 近景符号肖像；
- 侧脸或四分之三侧脸；
- 剪影；
- 背影；
- 人物局部；
- 服装 + 道具；
- 主配角组合；
- 男/女主关系构图；
- 对立角色 split composition；
- 群像星座式构图；
- 人物与物件/空间融合；
- 光影中的环境肖像。

Skill 应搜索并提取主人公、主演或关键角色的视觉特征：

- 面部结构或关键五官特征；
- 轮廓；
- 服装；
- 发型；
- 姿态；
- 道具；
- 色彩；
- 空间；
- 重复动作或标志性表情。

不要直接复制剧照、宣传照或官方海报。

一旦呈现电影人物，必须严格契合电影中的真实角色形象。可以进行美术化、风格化和适度变形，但不能捏造一个电影中不存在的角色形象，也不能用泛化的帅哥/美女替代真实角色。

角色呈现应更大胆、更正面：当影片识别度需要人物时，可以使用正脸美术肖像、四分之三侧脸、半身像、人物关系双人构图或关键角色群像。背影和远景小人只能作为特定海报策略之一，不能成为默认模式。

### 4.3.1 角色身份锁定

只要海报中出现具体电影角色，skill 必须先建立角色身份锁定，而不是只写“男主”“女主”或演员姓名。

角色身份锁定包括：

- 角色名；
- 演员/表演者；
- 电影名与年份；
- 角色关系；
- 角色在片中的年龄感、气质和行为姿态；
- 必须保留的脸部、发型、服装、道具、表情、场景线索；
- 禁止替换项，例如泛化帅哥/美女、现代时装版本、无关演员脸、电影中不存在的新角色。

提示词必须同时写明角色身份和电影身份，例如：

```text
《色，戒》(2007) 中的王佳芝，而不是泛化的 1940 年代旗袍女性。
```

### 4.3.2 参考图优先

如果生图模型支持参考图输入，涉及具体人物时必须优先使用角色参考图。优先级：

1. 用户提供的电影剧照或截图；
2. 官方剧照或官方预告片截图；
3. 制作剧照；
4. 可信电影数据库图片；
5. 可靠媒体文章图片；
6. 演员普通照片只能在缺少角色剧照时辅助脸部结构，不能作为主要角色参考。

如果当前 agent 具备联网、browser use、下载或视频截帧能力，skill 应先自动寻找并获取角色参考图，而不是默认要求用户上传。用户上传是 fallback，不是主路径。

自动获取参考图包括：

- 搜索角色在片中的官方剧照；
- 搜索官方预告片、官方片段或可访问视频中的角色画面；
- 搜索制作剧照；
- 搜索可信电影数据库或可靠媒体文章中的角色图片；
- 在环境允许时下载图片或截取预告片帧，作为本轮生图参考。

推荐查询：

```text
[电影名] [年份] [角色名] still
[电影名] [年份] [演员名] [角色名]
[电影名] official still [角色名]
[电影名] trailer [角色名] screenshot
[原名] [角色名] film still
```

如果自动获取失败、访问受限、图片质量太低，或当前生图模型/agent 无法把获取到的图片作为参考图输入，才询问用户是否愿意上传角色剧照。若用户不提供，只能走 `text_only` 身份锁定，并在提示词包和 manifest 中记录“角色相似度风险较高”。

不要把官方海报作为主要角色参考，以免生成结果复制现成海报。

自动获取必须遵守限制：不绕过登录墙、付费墙、验证码、反爬或版权保护；不把参考图当作最终输出素材直接拼贴；只作为本轮角色身份锚定或用户明确同意的本地参考库。

### 4.3.3 角色识别验收

生成后必须检查：

- 不看标题时，熟悉电影的人是否能认出这个角色；
- 是否使用了角色参考图；
- 如果 agent 具备联网/browser 能力，是否先尝试自动获取角色参考图；
- 如果没有使用参考图，是否记录了 `text_only` 风险；
- 是否出现了泛化人物脸、现代造型或不存在的角色；
- 角色是否服务海报隐喻，而不是只作为装饰人物。

如果角色不像，应优先重试，而不是直接接受：

1. 切换到参考图模式；
2. 减少人物数量，先锁定最重要的一两个角色；
3. 从全身改为脸部/半身；
4. 强化必须保留特征和禁止替换项；
5. 如果模型仍无法保留角色身份，改用非脸策略：手、服装、道具、剪影、物件身体融合或文字主导海报。

## 5. 文字策略

文字策略以效率和审美优先。带文字版本默认可由图像模型直接生成完整海报；后期排版是精确性兜底，而不是每次必走路径。

文字版本分为：

- `no_text`：无文字主视觉；
- `with_text`：优先由生图模型直接生成整张带文字海报，必要时后期加入片名、导演、年份等信息；
- `both`：同时输出两版。

带文字版最多包含：

- 片名；
- 原名；
- 导演/年份；
- 国家/地区；
- 一句策展短语。

文字渲染模式：

- `model_text`：生图模型直接生成文字，最快，适合普通单张生成；
- `no_text`：无文字版；
- `post_layout`：先生成无文字图，再由工具精确排版；
- `both`：同时生成无文字版和带文字版。

如果模型生成文字出现错字、乱码、语种错误或不可读，应重试更短文字，或切换到 `post_layout`。

带文字版不得改变整张海报的亮度、色彩、对比度、饱和度或影调。文字只能通过排版、文字阴影、描边、局部小底、局部微弱遮罩等方式保证可读性；禁止给整张海报加暗层、全局滤镜、全局渐变或整体降亮度。带文字版和无文字版应保持基本一致的整体视觉效果。

排版语言规则：

- 第一行：中文片名；
- 第二行：影片对应国家/语言的原名，例如英美电影用英文，法国电影用法文，德国电影用德文，日本电影用日文；
- 导演、国别都用中文；
- 策展短语默认中文，除非用户指定其他语言。

## 6. 输出结构

建议输出到用户确认的本地目录：

```text
CinephileWallpaper/
├── outputs/YYYY/MM/
│   ├── YYYY-MM-DD_HHMM_Title_visual.png
│   ├── YYYY-MM-DD_HHMM_Title_wallpaper_with_text.png
│   ├── YYYY-MM-DD_HHMM_Title_wallpaper_no_text.png
│   └── YYYY-MM-DD_HHMM_Title_manifest.json
└── history/history.json
```

### 6.1 用户可见交付

默认最终回复只包含：

- 生成完成状态；
- 海报/壁纸文件路径；
- 如可渲染，直接展示生成图片；
- 若用户要求设置桌面，则返回设置结果。

默认不向用户展示：

- 研究报告；
- 长视觉 brief；
- 完整 manifest JSON；
- 完整 image prompt；
- negative prompt；
- model notes；
- 来源链接列表；
- 中间参考图清单。

这些内容可以保存到本地 manifest 或 prompt package 文件，但不占用最终回复 token。

只有以下情况才展示提示词包或研究信息：

- 用户选择 `prompt_only`；
- 没有可用生图能力；
- 生成失败，需要用户拿提示词去其他模型；
- 用户明确要求查看、复制或修改提示词；
- 用户要求调试某次失败或不满意的生成。

### 6.2 Token 预算

Skill 应默认低 token 运行：

- 不长篇解释自己正在做什么；
- 不粘贴研究材料；
- 不输出多段设计说明；
- 不把 manifest 展示给用户；
- 不重复用户已确认的设置；
- 能用一轮设置解决的，不拆成多轮；
- 能静默保存的，不在最终回复展开。

Manifest 必须记录：

```json
{
  "film": {
    "title": "",
    "original_title": "",
    "original_title_language": "",
    "year": "",
    "director": "",
    "country": [],
    "source_urls": []
  },
  "visual_brief": {},
  "film_tone": {
    "color_system": "",
    "brightness": "",
    "contrast": "",
    "temperature": "",
    "medium_clues": [],
    "tonal_strategy": "",
    "rationale": "",
    "avoid_tone": []
  },
  "art_language": {
    "style_lane": "",
    "primary": "",
    "secondary": "",
    "movement_family": "modern_contemporary | classical | regional_traditional | material_process | counterpoint",
    "abstraction_mechanism": "",
    "semiotic_layers": [],
    "counterpoint_bridge": "",
    "rationale": "",
    "avoided_recent_lanes": []
  },
  "typography": {
    "title_zh": "",
    "original_title": "",
    "original_title_language": "",
    "director_zh": "",
    "country_zh": []
  },
  "character_reference": {
    "enabled": true,
    "characters": [],
    "identity_locks": [],
    "reference_mode": "auto_acquired_refs | user_uploaded_refs | image_references | text_only | none",
    "reference_images": [],
    "local_reference_paths": [],
    "acquisition_method": "browser_search | web_search | trailer_frame | user_upload | text_only | none",
    "visual_traits": [],
    "required_traits": {
      "face": [],
      "hair": [],
      "costume": [],
      "posture": [],
      "props": [],
      "expression": []
    },
    "forbidden_substitutions": [],
    "strategy": "",
    "source_urls": [],
    "auto_acquisition_attempted": false,
    "text_only_likeness_risk": false,
    "recognition_check": {
      "recognizable_before_title": false,
      "failure_recovery_used": false,
      "notes": ""
    }
  },
  "model": {
    "provider": "",
    "name": "",
    "generation_mode": "",
    "parameters": {}
  },
  "wallpaper_settings": {
    "width": 2560,
    "height": 1440,
    "size_source": "user | device_lookup | detected_current_screen | default",
    "output_dir": "",
    "language": "zh-CN",
    "language_source": "detected | user",
    "text_variants": ["with_text", "no_text"],
    "text_rendering_mode": "model_text | no_text | post_layout | both",
    "one_turn_setup": true,
    "global_text_overlay_used": false
  },
  "preference_memory": {
    "size_remembered": false,
    "output_dir_remembered": false,
    "preferences_path": ""
  },
  "prompt": {
    "image_prompt": "",
    "negative_prompt": "",
    "layout_notes": "",
    "model_notes": "",
    "portable_prompt": true,
    "style_escalation_used": false
  },
  "outputs": {
    "visual_image": "",
    "wallpaper_with_text": "",
    "wallpaper_no_text": "",
    "layout_html": ""
  },
  "created_at": ""
}
```

## 7. 版本演进

### v0.6

- 从 App 主体转向跨 agent skill。
- 确认不再以内置定时换壁纸为核心。
- 建立电影确认、尺寸确认、输出目录、文字版本、生图方式的基础流程。
- 当时要求每次返回提示词包；v0.9 起改为默认静默保存，按需展示。

### v0.8

- 新增影片影调研究：彩色/黑白、亮暗、对比度、色温、媒介质感和情绪类型。
- 新增影调反默认规则：严肃电影不自动等于阴暗、低饱和、黑底。
- 强化角色锚点：支持正脸美术肖像、四分之三侧脸、半身像、双人关系构图和关键角色群像。
- 强化角色准确性：涉及人物时必须契合电影中的真实角色形象，不能捏造不存在的角色。
- 新增角色身份锁定：出现具体角色时必须记录角色名、演员、片名年份、必要视觉特征和禁止替换项。
- 新增参考图优先：支持参考图的模型必须使用角色剧照/截图；不支持时询问用户上传，或记录 `text_only` 相似度风险。
- 新增自动参考图获取：具备联网/browser/download/截帧能力时，skill 默认先自动寻找角色剧照或预告片帧；用户上传作为 fallback。
- 新增偏好记忆：尺寸和输出目录可在用户确认后设为默认。
- 新增风格升级机制：当输出仍然常规插画化时，必须启用结构性的 `style_escalation`。
- 新增风格路由器：每次生成必须选择具体 `style_lane`，并避免连续重复同类艺术语言。
- 新增直接文字路径：带文字版可优先由图像模型直接生成，后期排版只作为精确性兜底。
- 新增一轮设置：电影确认后集中询问尺寸、保存位置、文字版本、生图方式和默认记忆。

### v0.9

- 默认海报优先交付：最终回复只给图片/路径和简短状态。
- 提示词包、研究信息、manifest 默认保存不展示。
- 只有 prompt-only、失败、调试或用户明确要求时才展示提示词/研究信息。
- 强化 token 节省策略：减少中间说明、文档输出和重复确认。

### v0.99

- 作为发布候选版。
- 默认启用 quick mode，减少手动确认。
- 尺寸、输出路径、文字模式、生图模式优先自动推断或读取偏好。
- 生图模式按可用能力自动路由，不再默认询问。
- 桌面设置只在用户明确要求或已保存为偏好时执行，不再每次询问。
- 风格路由器和角色参考图闸门纳入发布验收。

## 8. 非目标

v0.99 不做：

- 定时换桌面壁纸；
- 豆瓣海报自动抓取作为主路径；
- 账号系统；
- 云同步；
- 海报市场；
- 直接复制官方海报；
- 固定模仿某位在世设计师。
- 默认输出研究报告或长设计文档。

## 9. 验收标准

- 第一轮交互足够简洁，不做长篇功能介绍；
- 默认 quick mode 下，除非必要，不连续追问设置；
- 能处理同名电影歧义；
- 能询问并确认壁纸/海报尺寸；
- 能把尺寸、保存位置、文字版本、生图方式和默认记忆集中到一轮设置里；
- 能自动推断尺寸、保存位置、文字版本和生图模式；
- 能在用户同意后记忆默认尺寸；
- 能根据设备型号联网查询分辨率；
- 能确认本地输出目录；
- 能在用户同意后记忆默认输出目录；
- 能询问带文字、无文字或两者都要；
- 能在用户要求或 prompt-only 模式下返回可复制提示词；
- 成功生成图片时默认不展开提示词、研究报告或 manifest；
- 最终回复以图片/路径为主，token 占用低；
- 能避免所有海报都落入常规插画风格；
- 每次生成能记录并执行具体 `style_lane`；
- 能避免连续多次重复同一 `style_lane` 或同一风格大类；
- 能选择现当代、古典、地域传统、材料工艺或反差风格作为艺术语言；
- 能说明艺术语言如何揭示影片的主题、情绪或结构；
- 能产生抽象感、意境感和能指/所指层次，而不是只做剧情插图；
- 用户反馈常规插画化后，下一次必须启用 `style_escalation`；
- 能根据影片自身影调决定明暗、色彩和材质，不把所有严肃影片压成阴暗低饱和；
- 能通过多样化人物锚点增强电影识别度；
- 涉及具体人物时能建立角色身份锁定；
- 支持参考图时能把角色剧照/截图作为生图参考，而不是只靠文字描述；
- 具备联网/browser 能力时能先自动获取角色参考图；
- 自动获取失败或模型不支持参考图时，能提示用户上传角色剧照，并记录 `text_only` 相似度风险；
- 不反复生成“远景背影小人”；
- 能主动使用正脸美术肖像、四分之三侧脸、半身像、双人/群像等角色构图；
- 输出仍然是海报，不是写实剧照；
- 输出一律不是真人写实风格，而是美术/平面设计风格；
- 涉及人物时必须符合电影中实际角色形象，不捏造角色；
- 带文字版本遵守中文片名 + 原语种原名 + 中文导演/国别规则；
- 带文字版本可直接由生图模型生成；文字错误时切换到精确后期排版；
- 不为了后期文字刻意制造大面积留白，整体海报构图优先；
- 带文字版不得通过全局暗层、全局滤镜或整体降亮度来提升文字可读性；
- skill 文档不依赖 Codex 专属能力，其他 agent 可迁移使用。

## 10. 下一步

- 用《惊魂记》测试：角色锚点不只使用远景背影；
- 用《花样年华》测试：主角关系和服装/空间锚点；
- 用《千与千寻》测试：多角色与奇幻空间锚点；
- 用《小武》《绿洲》《复仇者联盟4》《横道世之介》《安德烈·卢布廖夫》测试影调差异，确保不会统一压暗；
- 用《银翼杀手2049》测试反差风格，例如中国水墨或中世纪手抄本，但必须保持电影识别度；
- 用《复仇者联盟4》测试波普、构成主义、壁画、挂毯等非普通插画路径；
- 继续扩展 poster grammar presets。
